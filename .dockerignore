# .dockerignore — chỉ đóng gói file web tĩnh vào image nginx.
# Loại thư mục lịch sử git (nguyên nhân build context nặng khiến COPY rớt)
# và các file dev không cần khi chạy web.

.git
.github
.gitignore
.claude
.impeccable
.netlify
.mcp.json

docs
tools
node_modules

# Ảnh nặng không được trang nào dùng — không đưa vào image
assets/img/media/hero-paceland.png
# Dữ liệu trang full dự án: chỉ prerender đọc lúc build, trình duyệt không tải
assets/data/du-an-chi-tiet
assets/data/phan-khu
assets/data/gio-hang

*.md
*.zip
netlify.toml

# Tooling phát triển (không phục vụ web)
package.json
package-lock.json
